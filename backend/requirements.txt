# PRISM backend. Lower bounds only - nothing here is pinned to an exact release.
#
#   py -m pip install -r backend/requirements.txt

fastapi>=0.115
uvicorn[standard]>=0.30

# The current SDK. NOT google-generativeai, which is retired and has a
# different, incompatible call shape.
google-genai>=1.0

pydantic>=2.7

# Durable application state. Local development uses Python's built-in SQLite
# driver; production uses PostgreSQL through Psycopg 3. Alembic owns the SQL
# schema so a container can upgrade before it begins serving requests.
SQLAlchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.2

# Required for multipart/form-data. Without it every UploadFile request 500s on
# arrival, before any handler code runs.
python-multipart>=0.0.9

python-dotenv>=1.0

# Verifying Supabase access tokens. `crypto` brings the RSA/EC backend needed
# for a project that publishes its keys rather than sharing a secret.
PyJWT[crypto]>=2.8
anyio>=4.4
reportlab

# Reading what the client sent. A scope in Word, a requirement list in Excel and
# a tender as a PDF are all quoted from as text - see
# app.shared.infrastructure.attachments.
# pypdf comes with the PDF export already.
python-docx>=1.1
openpyxl>=3.1
pypdf>=4.0

# Where a client's own files go, when a studio has somewhere to put them:
# DigitalOcean Spaces, over the S3-compatible API. Optional in practice -
# `app.features.intakes.infrastructure.files` imports it lazily, inside the
# Spaces backend, and falls
# back to the workspace's own folder when no credentials are configured, so the
# app imports and every check script runs on a machine that has never installed
# this.
#
# The floor is 1.36 rather than 1.34 for a specific reason: botocore 1.36
# changed `request_checksum_calculation` to default to `when_supported`, so
# PutObject - the one operation this module performs on the write path - began
# carrying `x-amz-checksum-crc32`. A `>=1.34` floor would have permitted both
# behaviours on the same line of code, and this repo has no lockfile to settle
# which one a given machine gets. Nothing here claims DigitalOcean refuses a
# checksummed put; the point is that a request shape should not depend on which
# side of a documented default change pip happened to resolve.
boto3>=1.36
